- CDP 消息统一用自增 id + pending Map + 超时 Promise 封装成 send(ws, method, params) 异步调用
- 通过 `url.includes('/attendance/human/rzAttendanceinfo/listByMonth')` 匹配目标接口，而非精确正则
- 错误处理一律抛 Error 并设置 `process.exitCode = 1`，由顶层 main().catch 统一退出